function C = C_calc(x0,th,N)

n = size(x0,1);
m = size(th,1);

C = zeros(n,(n+m),N);
C(:,:,1) = [ A_calc(x0,th) B_calc(x0,th) ];

for j = 2:N
    x_u
    C(:,:,j) = 
end
    
end
    
    C = zeros(3,3);
    for j = 1:m
        M = eye(3);
        
        if j <= m
            for k = (j+1):m
                M = M*A_calc(X(:,j),th);
            end
            M = M*B_calc(X(:,j),th);
        else
            M = B_calc(X(:,j),th);
        end 
        
        C = C+M;
    end
end